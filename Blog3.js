import React from "react";


function Blog3() {
    return (
        <div>
            <h1>React Routing with React Router</h1>
            <p>
                React Router is a standard library for routing in React applications. It allow for navigation between views of different components.
            </p>
            <h2>Key Concepts in React Router</h2>
            <ul>
                <li><strong>BrowserRouter:</strong>Manages the routing history.</li>
                <li><strong>Routes & Route:</strong> Define the different paths and their corresponding components.</li>
                <li><strong>NavLink:</strong>Provides navigation links with automatic styling.</li>
                <li><strong>useNavigation:</strong>Programmatically navigation between routes.</li>
            </ul>
            <p>
                React Router simplifies client-side navigation and enables building SPAs with ease.
            </p>
        </div>
    );
}

export default Blog3;